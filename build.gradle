apply plugin: 'java'
apply plugin: 'eclipse'
apply plugin: 'maven'

group = "ch.jass.model"
version = "1.0"

repositories {
	mavenLocal()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.+'
}
